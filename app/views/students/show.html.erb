
<h2> Here is the info for <%= @student.first_name %> <%= @student.last_name %>: </h2>

<h4><%= link_to "#{@student.id}", @student %> | First name: <%= @student.first_name %> | Last name: <%= @student.last_name %>
  | Address <%= @student.address %> | Age: <%= @student.age %></h4>
<%= link_to "Edit", edit_student_path(@student) %> <%= link_to "Delete", student_path(@student), method: :delete, data: { confirm: "Are you sure you want to delete this student?" } %>
